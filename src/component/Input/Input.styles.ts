import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  input: {
    height: 60,
    width: 200,
    borderColor: 'black',
    borderStyle: 'solid',
    borderWidth: 1,
    fontSize: 16,
  },
});

export default styles;
